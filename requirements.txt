# Core dependencies — all modules
pydantic>=2.0
opencv-python>=4.9
ultralytics>=8.0        # YOLO v8/v11

# PyTorch — install the correct build for your hardware:
#
#   MacBook Air / MacBook Pro (Apple Silicon M1/M2/M3):
#     pip install torch torchvision torchaudio
#     (installs MPS-enabled build automatically on macOS 12.3+)
#
#   MacBook Air / MacBook Pro (Intel):
#     pip install torch torchvision torchaudio
#     (same command — CPU-only build on Intel Mac)
#
#   Windows / Linux with NVIDIA GPU (CUDA 11.8):
#     pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu118

# Tracking
# deepsort used in run_live.py (single-camera mode):
# deep-sort-realtime>=1.3

# ANPR OCR — works on CPU (Mac) and GPU (NVIDIA)
easyocr>=1.7

# Face Recognition — works on Mac via CoreML / CPU
insightface>=0.7
onnxruntime>=1.17          # CPU / Intel Mac
# On Apple Silicon (M1/M2/M3), install instead:
# onnxruntime-silicon       (CoreML-accelerated, optional)
faiss-cpu>=1.7             # vector search for face matching

# Alarm Manager / API
fastapi>=0.111
uvicorn[standard]>=0.29
python-multipart>=0.0.9
sqlalchemy>=2.0

# Testing
pytest>=8.0
pytest-asyncio>=0.23

# Code quality (enforced by pre-commit)
black>=24.0
ruff>=0.4

# Utilities
pyyaml>=6.0
python-dotenv>=1.0
